# ChatFlow360 - Environment Variables
# Copy this file to .env.local and fill in the values

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (server-side only — NEVER prefix with NEXT_PUBLIC_)
# Used as global fallback when org has no per-org API key configured
OPENAI_API_KEY=

# Encryption (server-side only — NEVER prefix with NEXT_PUBLIC_)
# 64 hex characters (32 bytes) for AES-256-GCM encryption of API keys
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Database (Prisma)
DATABASE_URL=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000  # Production: https://app.chatflow360.com
